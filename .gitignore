# Environment variables — NEVER commit these
.env
.env.local
.env.*.local
!.env.example

# Shopify CLI local state (tokens, deploy bundles)
.shopify/

# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.pnpm-store/

# OS
.DS_Store
Thumbs.db
Desktop.ini

# Editor (keep shared .vscode/* if you add them; ignore personal overrides)
.vscode/settings.json
.idea/
*.swp
*.swo

# Theme / build artifacts
dist/
*.tsbuildinfo

# Import data — generated outputs (source CSV stays tracked)
data/import-history.json

# Logs & temp
*.log
.cache/
tmp/
temp/

# Secrets & keys (defense in depth)
*.pem
*.key
.env.backup
